from ape import accounts
# TODO(brownie-to-ape): replace Brownie priority_fee with Ape provider fee configuration

accounts.add("0x1234...")  # TODO(brownie-to-ape): accounts.add(key) not valid in Ape; use accounts.load("account-name") after: ape accounts import <name>
# TODO(brownie-to-ape): replace Brownie priority_fee with Ape provider fee configuration
