{
  "name": "brownie-to-ape",
  "private": true,
  "description": "Brownie to Ape Framework migration codemods (ApeWorX).",
  "scripts": {
    "test": "pnpm -r test",
    "check-types": "pnpm -r check-types",
    "changeset": "changeset",
    "version-packages": "changeset version && bash scripts/sync-codemod-versions.sh"
  },
  "packageManager": "pnpm@10.19.0",
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  }
}
